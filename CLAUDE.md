# CLAUDE.md

**Changing arena-mobile**, a token, the emit, a gate, a theme, a release:
[`AGENTS.md`](./AGENTS.md).

This file carries no rule of its own. It exists because a harness that loads `CLAUDE.md` and
nothing else would otherwise land nowhere. There is one branch here and one router, so the
whole of it is stated once, on the other side of that link.
